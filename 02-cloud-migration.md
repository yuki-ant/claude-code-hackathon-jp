# シナリオ2. クラウド移行

## 「リフトとシフト、そして午前4時の呼び出し」

Contoso Financial は3つのワークロードをオンプレミスで動かしています。顧客向けの Web アプリ、夜間バッチの照合ジョブ、そして5つのチームが直接クエリを投げるレポーティングデータベースです。CFO はすでにクラウド契約にサインを済ませました。一方 CTO は「リフト&シフトではなくクラウドネイティブで」と言い、コンプライアンス部門はデータ所在地の管理を求め、SRE はとにかく夜くらい眠りたいと思っています。誰一人、足並みが揃っていません。

移行先のクラウドと移行パターンは自由に選べます。ただし、実環境へのデプロイは行いません。その代わりに、本番相当のアーキテクチャでローカルに動く、クラウド対応のアーティファクトを作ってください。具体的には、各サービスをクラウドのプリミティブに対応付けた Docker Compose です。S3 の代役には MinIO、RDS には Postgres、ElastiCache には Redis を立て、命名もそれに合わせます。

完成した IaC は監査人が読み、ADR は CTO が読み、ランブックは午前4時の運用チームが実行します。この3種類の読者すべてを想定して設計してください。

---

## チャレンジ

以下はチェックリストではなく道しるべです。取り組みたいものを選んでください。

1. **メモ。** *(PM/BA)* 意思決定メモを書きます。まずリフト&シフトで移して後から最適化するのか、それとも移行と同時にリファクタリングするのか。どちらかに立場を決めて論拠を示し、受け入れるリスクと、そのリスクを誰が背負うのかまで名指ししてください。分量は1ページ、逃げ口上は禁止です。読者席には法務と CFO が座っています。

2. **ディスカバリー。** *(アーキテクト)* 現状の実態をあぶり出します。3つのワークロードとその設定、そして誰も文書化してこなかった厄介な相互依存(ハードコードされた IP、共有ファイルシステムのマウント、キャッシュを温めるためにエンドポイントを叩き続ける cron)を洗い出してください。細部を引き出すには、Claude を相手にステークホルダー・インタビューをロールプレイしてみるのも有効です。ここで掘り当てた事実は、次のチャレンジのアーキテクチャ選定に目に見える形で反映させてください。

3. **選択肢。** *(アーキテクト)* 選んだクラウド上でのターゲットアーキテクチャ候補を数案挙げ、コスト、リスク、スピード、運用性の観点で採点したうえで、推薦案を添えます。語るときは実在のサービス名を使ってください(ECS か EKS か、App Service か Container Apps か。選択はお任せします)。推薦案は ADR としてコミットします。このあたりから、ワークロードごとの `CLAUDE.md` が効き始めます。バッチのフォルダと Web アプリのフォルダとでは、誰かが編集するときに Claude へ与えるべきガイダンスが違うからです。

4. **コンテナ。** *(開発)* Web アプリをコンテナ化します。マルチステージビルド、非 root ユーザー、ヘルスチェック用エンドポイントを備え、`docker compose` でローカル起動できること。さらに、同じイメージをリビルドせず、設定の差し替えだけで移行先クラウドにデプロイできる形を目指します。

5. **土台。** *(プラットフォーム)* ターゲットアーキテクチャ全体を Infrastructure-as-code で書きます。実際のクラウドにデプロイすることはありませんが、*読んだときに正しい*ものでなければなりません。冪等に書かれていること、シークレットがハードコードされていないこと、そしてステートファイルの扱いが「リポジトリにコミットする」以外の答えになっていること。Claude が IaC に平文シークレットを書き込もうとしたら決定論的にブロックする `PreToolUse` フックは、手軽なわりに効果の高いガードレールです。あわせて `CLAUDE.md` に「X にはシークレットマネージャーを優先する」というプロンプトを書き、なぜブロックにはフックを使い、優先の指示はプロンプトで済ませたのかを短い ADR にまとめてください。

6. **証明。** *(品質)* 「移行成功」の定義そのものとなる検証スイートを作ります。中身はスモークテスト、コントラクトテスト、データ整合性チェックです。今はローカルの代役スタックに対して走らせ、カットオーバー後には本物のクラウドに対してもう一度走らせます。少なくともいくつかのアサーションでは、「ディスカバリー」で見つけた文書化されていない事柄を狙い撃ちしてください。そうしないと、検証がただのお芝居になってしまいます。

7. **スコアカード。** *(品質)* Claude が生成する IaC や移行系アウトプットを測る eval ハーネスです。同じプロンプトに同じワークロードを渡しても、毎回同じ Terraform が出てくるとは限らないからです。正しいと分かっている IaC スニペットのゴールデンセット、既知の悪いパターン(権限が過剰な IAM、ハードコードされたシークレット、タグの欠落、開きっぱなしのセキュリティグループ)、そして参照用の移行計画を用意します。測るのは、Claude の IaC がゴールデンスタンダードに合致するか、悪いパターンを正しく指摘できるか、そしてフックにブロックされるはずの提案を自信満々に出してくる頻度です。CI で回しておけば、非対話モードの Claude によるレビューを、雰囲気ではなく数字で説明できるようになります。

8. **切り戻し。** *(発展)* ワークロード別、カットオーバー段階別のロールバック計画を書きます。図ではなく、正確な手順書としてです。誰も書きたがらないのに、午前4時には全員が欲しがる、あの文書のことです。机上の空論で終わらせないよう、最低1回は通しで確認してください。

9. **調査隊。** *(発展、エージェント)* Task サブエージェントを使った並列ディスカバリーです。ワークロード(Web アプリ、バッチジョブ、レポーティングデータベース)ごとにサブエージェントを1つ立て、それぞれが設定を読み、依存関係を探り、構造化された現状レポートを出力します。それをコーディネーターが1つのディスカバリードキュメントに統合します。サブエージェントはコーディネーターのコンテキストを引き継がないので、対象スコープは各 Task プロンプトの中で明示的に渡してください。うまくいけば、統合後のアウトプットには一発の分析では見落とすようなワークロード横断の結合が少なくとも1つ浮かび上がり、「選択肢」でのアーキテクチャ判断も目に見えて研ぎ澄まされるはずです。

---

**このシナリオで鍛えられる認定試験ドメイン:**

- **Claude Code の設定。** ワークロードごとの `CLAUDE.md`。IaC レビューのための CI 上の非対話モード Claude。カットオーバー手順のための Plan Mode。
- **コンテキスト管理。** IaC のシークレットに対するフックとプロンプトの併用。カットオーバー判断のエスカレーションルール。IaC eval における層化サンプリングと誤確信率(「スコアカード」経由)。
- **ツール設計。** ローカルのクラウド代役スタックの上に立てる MCP サーバーと、各ツールが*何をしないか*までエージェントに教える説明文。
- **エージェントアーキテクチャ。** ワークロードを並列に調査する Task サブエージェントと、各 Task 呼び出しでの明示的なコンテキスト受け渡し(任意、「調査隊」経由)。
