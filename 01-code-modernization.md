# シナリオ1. コードモダナイゼーション

## 「モノリス」

Northwind Logistics のビジネスは、古いシステムの上で回っています。いちおう動いてはいるものの、作った人たちはとうに会社を去り、残されたドキュメントといえば、古びた Word ファイルの詰まったフォルダが1つあるだけ。そんな状況で経営陣は、中身を定義しないまま「モダナイゼーション」を承認してしまいました。皆さんの仕事は、ビッグバン・リライトに頼らなくてもこのシステムを安全に進化させられる、と証明することです。

言語も時代設定も、アーキテクチャも分割戦略も、好きに選んでください。ルールは1つだけあります。直し甲斐を感じるくらい醜いものを生成してください。

---

## レガシーを選ぶ(自作も歓迎)

| 系統 | Claude が生成してくれるもの |
|---|---|
| **PHP 5 モノリス** | `index.php`、インラインで連結される SQL 文字列、グローバル変数に置かれたセッション |
| **2010年頃のエンタープライズ Java** | Spring の XML 設定、`AbstractSingletonProxyFactoryBean`、WebLogic 上の WAR |
| **ストアドプロシージャ・アーキテクチャ** | ビジネスロジック*そのもの*と化した40本の T-SQL プロシージャ。アプリは薄い殻 |
| **初期 Node のコールバック地獄** | Express 3、6段ネストのコールバック、Mongoose の pre-save フックに潜むロジック |
| **Rails 2 の荘厳なモノリス** | 肥大化したモデル、口に出せないことをしている `lib/`、cron と rake ジョブの組み合わせ |
| **Classic ASP / VB6** | COM コンポーネント、ADO レコードセット、インライン VBScript |
| **SOAP サービスのもつれ** | WSDL ファイルの山と、実態はただのキューでしかない ESB |
| **COBOL とバッチ** | 固定長ファイル、JCL、中断できない夜間バッチ |

**モダナイズの方向性:** ストラングラーフィグ、コンテナ化してそのまま出荷、イベント駆動、API ファサード、サーバーレスへの切り出し、DB ファーストの分割。どれを選ぶかはお任せします。

---

## チャレンジ

以下はチェックリストではなく道しるべです。取り組みたいものを選んでください。

1. **ストーリー。** *(PM)* 本当に重要ないくつかのビジネスケイパビリティに絞って、ユーザーストーリーを書きます。受け入れ基準は、テスターがそのまま実行に移せるくらい具体的なものにしてください。ステークホルダーの間で優先順位は食い違うはずですが、丸く収めようとせず、食い違いそのものを明示的に記録します。切れ味のよいストーリーが揃っていれば、後で Claude にそこからテストの足場を組ませる作業も楽になります。

2. **患者。** *(アーキテクト)* レガシーモノリスを生成します。共有データベース、循環依存、ゴッドクラスの1つや2つ、データベーストリガーに隠れたビジネスロジック。思わず顔をしかめてしまう部分まで含めて、リアルに作り込んでください。後続の作業が面白くなるかどうかは、この醜さにかかっています。生成を省略したい場合は、後述の「持ち込みモノリス」をどうぞ。

3. **地図。** *(アーキテクト)* 分割計画を、スライドではなく ADR として書きます。継ぎ目(シーム)に名前を付け、各サービスをサイズではなく切り出しリスクの高さで順位付けしてください。「あえて*やらない*と決めたこと」のセクションも忘れずに。ここでは3階層の `CLAUDE.md` が力を発揮します。ユーザーレベルには個人の好みを、プロジェクトレベルにはコードベースの規約を書き、さらにディレクトリレベルを使えば、モノリスのルートと新サービスのルートに、それぞれの事情に合ったコンテキストを与えられます。

4. **ピン留め。** *(テスター)* 誰かが手を入れる前に、モノリスの特性テスト(characterization test)を書きます。正しさを検証するテストではなく、バグも含めた現状の挙動をそのまま固定するテストです。将来誰かが意図せず挙動を変えてしまったとき、何が変わったのかを失敗メッセージが正確に教えてくれる状態を目指します。

5. **切り出し。** *(開発)* きれいな API コントラクトを備えた最初のサービスを切り出します。モノリスは動き続け、切り出したサービスも動き、しかもその両方を1回のテスト実行で証明できること。つまり、特性テストスイートと新しいコントラクトテストが、同じコミットで揃ってグリーンになっていれば合格です。

6. **フェンス。** *(開発/テスター)* 新旧の間に腐敗防止層(anti-corruption layer)を設けます。モノリスのデータモデルを、新サービスの公開インターフェースに漏れ出させてはいけません。モノリスのフィールド名が新サービスの API に現れた瞬間に派手に落ちるテストを置いておくと、よい強制装置になります。Claude が境界をまたいで書き込むこと自体も防ぎたければ、`PreToolUse` フックを使って決定論的に強制できます。あわせてプロジェクトの `CLAUDE.md` に「X には新サービスを優先する」というプロンプトを書き、なぜハードブロックにはフックを使い、優先の指示はプロンプトで済ませたのかを短い ADR にまとめてください。

7. **スコアカード。** *(品質)* LLM によるリファクタリングそのものを測る eval ハーネスを作ります。同じプロンプトに同じモジュールを渡しても、毎回同じ出力が返ってくるとは限らないからです。正解の分かっている切り出し例のゴールデンセット(生成したモジュールに「正しいシーム」「誤ったシーム」のラベルを付けたもの)を用意し、挙動が保たれているかどうかのチェックには特性テストスイートを使います。測るのは3点です。Claude が正しい境界を提案できるか、Claude のリファクタリング後も特性テストが通り続けるか、そして間違った答えに高い確信度を付けてしまう頻度はどのくらいか。これを CI で回しておけば、Claude が提案するすべての変更に根拠のある数字が付き、モダナイゼーションの進め方が雰囲気任せではなくなります。

8. **週末。** *(発展)* 午前3時に呼び出された運用チームが本当にそのとおり実行できる、カットオーバー・ランブックを書きます。手順、ロールバックの発動条件、判断ツリーまで含めてください。机上の空論で終わらせないために、少なくとも1回はリハーサルしておきましょう。

9. **斥候。** *(発展、エージェント)* Task サブエージェントを使ったファンアウト分析です。「地図」で挙げた候補シームごとにサブエージェントを1つ立て、それぞれが独立に切り出しリスク(結合度、テストカバレッジ、データモデルの絡まり具合、ビジネス上の重要度)を採点して、構造化された判定を返します。それをコーディネーターがランキングに集約します。サブエージェントはコーディネーターのコンテキストを引き継がないので、対象スコープは各 Task プロンプトの中で明示的に渡してください。最後に、エージェントが作ったランキングを「地図」で人間が書いたものと突き合わせ、どこが一致し、どこが食い違い、なぜ食い違ったのかを記録します。

---

## オプション: 持ち込みモノリス

レガシーの生成は省きたい、という方のために既製品を用意しています。

[https://github.com/rishikeshradhakrishnan/spring-music](https://github.com/rishikeshradhakrishnan/spring-music)

Spring Music は Cloud Foundry 向けに作られた Spring Boot のサンプルアプリで、Bean プロファイルと Spring Cloud Connectors を使い、同じドメインオブジェクトをリレーショナル、ドキュメント、キーバリューの各ストアに保存します。これを使う場合は、生成のチャレンジを飛ばして構いません。

---

**このシナリオで鍛えられる認定試験ドメイン:**

- **Claude Code の設定。** マルチモジュールのレガシーをまたぐ3階層の `CLAUDE.md`。切り出しプレイブックのためのカスタムコマンドとスキル。
- **コンテキスト管理。** サービス境界を守るフックとプロンプトの併用と、どちらをどちらにしたのかを説明する ADR。リファクタリング eval における層化サンプリングと誤確信率(「スコアカード」経由)。
- **エージェントアーキテクチャ。** 切り出しリスクを並列で採点する Task サブエージェントと、各 Task 呼び出しでの明示的なコンテキスト受け渡し(任意、「斥候」経由)。
