# シナリオ3. データエンジニアリング

## 「沼」

Fabrikam Retail には7つのソースシステムがあります。POS、e コマース、ロイヤルティ、CRM、それに買収のたびに抱え込んだ3つ。どのシステムに聞いても、「顧客」が何を指すのかの答えが揃いません。同じ人物に4つの ID が振られ、氏名の表記は2通りあります。分析チームはとっくにあきらめて、何もかも CSV エクスポートから組み立てている始末です。そこへ新任の CDO がやってきて、単一の信頼できる情報源を作れと言っています。

アーキテクチャとスタックは自由です。レイクハウスでも、ウェアハウスでも、データメッシュでも、お好きなものを選んでください。

---

## チャレンジ

以下はチェックリストではなく道しるべです。取り組みたいものを選んでください。

1. **カオス。** *(開発)* いくつかのソースにまたがるリアルなサンプルデータを作ります。バラバラのスキーマ、衝突するキー、エンコーディングの問題、タイムゾーンのバグ、一見しただけでは気づけない重複。いかにも合成データというノイズではなく、実システムが吐き出す類の悪いデータを作るよう、Claude に明示的に指示してください。「よくできた悪いデータ」と「出来の悪い悪いデータ」を対比する few-shot の例をいくつか見せれば、その違いを教え込めます。後のマッチングやデータ品質の作業に取り組む価値が生まれるかどうかは、このリアルさにかかっています。

2. **設計図。** *(アーキテクト)* ターゲットアーキテクチャを描きます。レイヤー、ゾーン、保持期間、PII のルール、そして誰が何を読めるのか。「あえてやらないと決めたこと」のセクションを付けて、ADR としてコミットしてください。ここでも3階層の `CLAUDE.md` が効いてきます。リポジトリのルートには全体のパターンを、ゾーンごとの階層には raw、conformed、curated で異なるルール(変更可否、保持期間、PII)を、ユーザーレベルには個人の好みを書きます。

3. **取り込み。** *(開発)* 形の異なる数ソース(バッチファイル、CDC ストリーム、不安定な API)のインジェストを作り、すべてリネージメタデータ付きで raw レイヤーに着地させます。パースして着地させるステップは、検証リトライループで包んでください。Claude が半構造化された入力をスキーマに沿ってパースし、構造化バリデーターがそれをチェックし、失敗したら具体的なエラーをフィードバックして Claude に最大 N 回リトライさせる、という流れです。行ごとのリトライ回数とエラー種別をログに残しておけば、その数字が後々の証拠になります。

4. **顧客。** *(開発/アーキテクト)* ソースを横断して顧客エンティティをマスタリングします。サバイバーシップルールを明示的に定め、確信度スコア付きのゴールデンレコードを作ってください。マッチャーには、少数の鋭い境界例を与えて教えます。そのうち少なくとも1つはネガティブケース(よく似ているのに同一人物ではない2レコードと、そう判定する根拠)にしてください。切れ味のよい境界例が2つあれば、「保守的に判定せよ」と1ページ書き連ねるよりも確実に効きます。

5. **トリップワイヤー。** *(品質)* データ品質チェックを仕掛けます。対象はスキーマドリフト、null の急増、ボリューム異常、参照整合性です。チェックごとに、パイプラインを止めるのかアラートに留めるのかを決め、そのルールを文書化してください。これらは決定論的なガードレールなので、プロンプトではなくコードで強制します。スキーマコントラクトを通過するまで curated ゾーンへの書き込みをブロックする `PreToolUse` フックは、自然な選択です。

6. **カタログ。** *(PM/BA)* コアエンティティのカタログエントリを、エンジニアではなくアナリストに向けて書きます。これは何なのか、どこから来たのか、信頼してよいのか、そして*ここでの*「顧客」は何を意味するのか。各エントリは上流のコントラクトにリンクし、プロデューサーが実際に何を約束しているのかをアナリストが確認できるようにしてください。

7. **スコアカード。** *(品質)* エンティティマッチャーの eval ハーネスです。マッチ、非マッチ、判定不能のラベルを付けたペアでゴールデンデータセットを作り、few-shot プロンプトで教えようとしている境界ケースもそこに含めます。メトリクスは precision、recall、そして誤確信率(マッチャーが「高確信」と言いながら外している頻度)です。簡単なケースにスコアが支配されないよう、層化サンプリングを使ってください。CI で回しておけば、CDO に「これはどのくらい当てになるの」と聞かれたときに、根拠のある1つの数字で答えられます。

8. **トレース。** *(発展)* リネージをエンドツーエンドで通します。レポート上の1つの不正な値を起点に、途中の変換まで含めて、プログラムからソースの行まで遡れるようにしてください。データプラットフォームの上に立てる MCP サーバー(`preview_table`、`trace_lineage`、`find_record`、`get_source_schema`)が自然にはまります。入力フォーマット、エッジケース、そして各ツールが*何をしないか*まで書いた説明文を付けておけば、まっさらな Claude セッションでも一発で正しいツールを選べます。

9. **スウォーム。** *(発展、エージェント)* Task サブエージェントを使った並列プロファイリングです。「カオス」で作ったソースごとにサブエージェントを1つ立て、それぞれがデータ品質(完全性、鮮度、キーのカバレッジ、異常件数、PII の露出範囲)を採点して構造化レポートを出力します。それをコーディネーターが1枚の「沼の健康状態」ダッシュボードに集約します。サブエージェントはコーディネーターのコンテキストを引き継がないので、コンテキストは各 Task プロンプトの中で明示的に渡してください。各サブエージェントにプロンプトで何を渡したのかをそのまま見せることも、成果物のうちです。そうしておけば、タスク分割の設計をレビュアーが読み解けるようになります。

---

## オプション: ゼロからではなく、データから始める

- **AdventureWorks for Postgres**([github.com/lorint/AdventureWorks-for-Postgres](https://github.com/lorint/AdventureWorks-for-Postgres))。定番のリテールスキーマで、顧客エンティティの作業と相性が良いでしょう。
- **Sentinel KYC**([github.com/beck-source/sentinel-kyc](https://github.com/beck-source/sentinel-kyc))。コンプライアンスの観点を備えた KYC データです。API キーが必要です。

これらを使う場合、チャレンジ1の生成パートは省いて構いませんが、検分パートは省かないでください。すでに中に潜んでいるノイズを探し出して、文書化しましょう。

---

**このシナリオで鍛えられる認定試験ドメイン:**

- **プロンプトエンジニアリング。** エンティティマッチングのための few-shot 境界例(ネガティブケースを最低1つ含む)。インジェストでの検証リトライループ。
- **ツール設計。** リネージとプレビューのツールを備えた、データプラットフォーム上の MCP サーバー。まっさらな Claude セッションでも一発で正しいツールを選べる説明文。
- **コンテキスト管理。** ゴールデンレコードにおけるフィールド単位の確信度。フックで強制する curated ゾーンのガードレールと、プロンプトレベルのゾーン優先指示の併用。マッチャー eval における層化サンプリングと誤確信率(「スコアカード」経由)。
- **エージェントアーキテクチャ。** ソースを並列でプロファイリングする Task サブエージェントと、各 Task 呼び出しでの明示的なコンテキスト受け渡し(任意、「スウォーム」経由)。
